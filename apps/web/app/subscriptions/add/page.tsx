"use client";
import React from "react";

export interface PageProps {
}

const Page = ({}: PageProps) => {
   return (
      <div>
         Add a new subscription.
      </div>
   );
};

export default Page;