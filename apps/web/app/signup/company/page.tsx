import React from "react";

export interface PageProps {
}

const Page = ({}: PageProps) => {
   return (
      <div>
         Create an account for company.
      </div>
   );
};

export default Page;